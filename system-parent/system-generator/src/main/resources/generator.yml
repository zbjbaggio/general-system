# 代码生成
gen:
  # 作者
  author: eason
  # 默认生成包路径 system 需改成自己的模块名称 如 system monitor tool
  packageName: org.general.system
  #模块名称
  moduleName: system
  # 自动去除表前缀，默认是true
  autoRemovePrefix: true
  # 表前缀(类名不会包含表前缀)
  tablePrefix: t_